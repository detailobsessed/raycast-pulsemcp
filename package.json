{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "pulsemcp",
  "title": "PulseMCP",
  "description": "Search and discover MCP servers from the PulseMCP registry. Find Model Context Protocol servers with stats, connection options, and more.",
  "icon": "extension-icon.png",
  "author": "ismi",
  "categories": [
    "Developer Tools",
    "Data"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "search-servers",
      "title": "Search MCP Servers",
      "subtitle": "PulseMCP",
      "description": "Search and discover MCP servers from the PulseMCP registry",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.103.10",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@types/node": "25.0.3",
    "@types/react": "19.2.7",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "check": "biome check --write",
    "lint": "biome check",
    "test": "bun run src/api.test.ts",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`bun run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "bunx @raycast/api@latest publish"
  },
  "platforms": [
    "macOS"
  ]
}
